import { FC } from 'react';
import MainContent from '../components/ui/MainContent';

interface LoginPageProps {}

const LoginPage: FC<LoginPageProps> = () => {
  return <MainContent>LoginPage</MainContent>;
};

export default LoginPage;
